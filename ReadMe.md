To Setup 

1) close any communcation to FC (Qgroundcontrol must be closed)
2) Run the commands in mav_proxy.txt first ot find port then the full command
3) Open Qgroundcontrol app
4) run main.py in a seperate terminal
